<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elice.boardproject.admin.mapper.AdminMapper">
    <!--전체 회원 정보 조회-->
    <select id="findAllProfiles" resultType="com.elice.boardproject.admin.entity.Admin">
        SELECT u.user_id, u.user_name, u.contact, u.email, u.user_pw, u.reg_date, u.mod_date, u.login_lock, ua.authorities
        FROM users u
        LEFT JOIN users_auth ua ON u.user_id = ua.user_id
    </select>

    <!--회원 삭제-->
    <delete id="deleteProfileByUserId">
        DELETE
        FROM users
        WHERE user_id = #{userId}
    </delete>

    <!--회원 선택 삭제-->
    <delete id="deleteProfilesByUserIds">
        DELETE
        FROM users
        WHERE user_id IN <foreach item="userId" collection="userIds" open="(" separator="," close=")">
              #{userId}
        </foreach>
    </delete>

    <!--로그인 (잠금/해제)-->
    <update id="toggleLoginLock">
        UPDATE users
        SET login_lock = #{newLockStatus}
        WHERE user_id = #{userId}
    </update>

    <!-- 필터 및 검색 조건에 따른 회원 조회 -->
    <select id="findFilteredProfiles" resultType="com.elice.boardproject.admin.entity.Admin">
        SELECT u.user_id, u.user_name, u.contact, u.email, u.user_pw, u.reg_date, u.mod_date, u.login_lock, ua.authorities
        FROM users u
                 LEFT JOIN users_auth ua ON u.user_id = ua.user_id
        WHERE (#{role} IS NULL OR #{role} = '' OR ua.authorities = #{role})
          AND (#{loginLock} IS NULL OR u.login_lock = #{loginLock})
          AND (u.user_id LIKE CONCAT('%', #{keyword}, '%')
            OR u.user_name LIKE CONCAT('%', #{keyword}, '%')
            OR u.contact LIKE CONCAT('%', #{keyword}, '%')
            OR u.email LIKE CONCAT('%', #{keyword}, '%')
            OR #{keyword} IS NULL)
    </select>
</mapper>
