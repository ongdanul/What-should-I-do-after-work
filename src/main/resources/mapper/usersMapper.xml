<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elice.boardproject.user.mapper.UsersMapper">
    <select id="existsByUserId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users
        WHERE user_id = #{userId}
    </select>

    <insert id="registerUser">
        INSERT INTO users (user_id, user_pw, user_name, contact, email)
        VALUES (#{userId}, #{userPw}, #{userName}, #{contact}, #{email})
    </insert>

    <select id="findByUserId" resultType="com.elice.boardproject.user.entity.Users">
        SELECT * FROM users
        WHERE user_id = #{userId}
    </select>

    <update id="loginLock">
        UPDATE users
        SET login_lock = #{loginLock}
        WHERE user_id = #{userId}
    </update>

    <select id="findUser" resultType="String">
        SELECT user_id FROM users
        WHERE user_name = #{userName}
        AND contact = #{contact}
    </select>

    <select id="findPasswordHash" resultType="String">
        SELECT userPw FROM users
        WHERE user_id = #{userId}
    </select>


</mapper>