<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elice.boardproject.post.mapper.PostMapper">

    <!-- post 목록 전체 조회 -->
    <select id="findAll" resultType="map">
        SELECT *
        FROM post
        WHERE 1=1
        <if test="description != null and description != ''">
            AND
            <choose>
                <when test="filter == 'post_title'">post_title</when>
                <when test="filter == 'post_content'">post_content</when>
            </choose>
            LIKE CONCAT('%', #{description}, '%')
        </if>
    </select>

    <!-- post 목록 단건 조회 -->
    <select id="detail" resultType="com.elice.boardproject.post.entity.Post">
        SELECT *
        FROM post
        WHERE post_id = #{postId}
    </select>

    <!-- post 등록 -->
    <insert id="insert">
        INSERT INTO post (user_id, board_id, post_title, post_content)
        VALUES (#{userId}, #{boardId}, #{postTitle}, #{postContent})
    </insert>

    <!-- post 수정 -->
    <update id="update">
        UPDATE post
        <set>
            <if test="postTitle != null">
                post_title = #{postTitle},
            </if>
            <if test="postContent != null">
                post_content = #{postContent},
            </if>
            <if test="modDate != null">
                mod_date = NOW()
            </if>
        </set>
        WHERE post_id = #{postId}
    </update>

    <!-- post 삭제 -->
    <delete id="delete">
        DELETE
        FROM post
        WHERE post_id = #{postId}
    </delete>
</mapper>